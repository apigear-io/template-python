from fastapi import Depends, FastAPI, Header, HTTPException

{% for module in system.modules %}  
import {{module.name | identifier}}.http.routes
{% endfor %}

app = FastAPI()

{% for module in system.modules %}  
app.include_router(
    router={{module.name|identifier}}.http.routes.router,
    prefix="/{{module.name}}"
)
{% endfor %}

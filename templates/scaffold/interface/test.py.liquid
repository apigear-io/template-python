{% assign class = interface.name %}
from {{module.name|identifier}}.{{interface.name|identifier}} import {{interface.name}}

class Test{{class}}:
{% for operation in interface.operations %}

    def test_{{operation.name}}(self):
        o = {{interface.name}}()
        o.{{operation.name}}(
{% for param in operation.params -%}    
          {{param|pyDefault}}{% unless forloop.last %}, {% endunless %}
{% endfor -%}
        )
{% else %}
      pass
{% endfor %}
